## 1. 命令入口与参数模型

- [x] 1.1 新增 `edr web-site-scan` 命令入口并接入统一扫描框架
- [x] 1.2 定义并校验请求参数 `groups`、`hostname`、`ip`、`port`、`proto`、`type`、`rootPath`
- [x] 1.3 实现中文 CLI 提示文案与 UTF-8 编码约束

## 2. 跨平台采集适配器

- [x] 2.1 设计站点采集统一接口并拆分 Windows/Linux 适配器
- [x] 2.2 实现“非外部命令”采集路径（进程内 API/配置解析）
- [x] 2.3 采集并映射站点核心字段（进程、协议、端口、服务用户、路径等）

## 3. 数据模型与字段规范

- [x] 3.1 定义输出主模型并覆盖需求中的全部返回字段
- [x] 3.2 实现 `domains`、`virtualDir`、`root` 的标准化对象映射
- [x] 3.3 将 `internalIp`/`externalIp` 改为 `internalIpList`/`externalIpList` 数组输出，并保留 `displayIp`

## 4. 输出与导出能力

- [x] 4.1 实现 JSON 输出并校验字段完整性与空值回退策略
- [x] 4.2 实现 Excel 导出并固定嵌套字段展平策略
- [x] 4.3 补充输出格式切换参数与导出路径处理

## 5. 验证与回归

- [x] 5.1 编写单元测试覆盖参数过滤与契约字段校验
- [x] 5.2 编写跨平台集成测试覆盖 Windows/Linux 采集流程
- [x] 5.3 验证“仅信息收集，无风险分析”边界不被破坏
