## 1. 命令与数据契约

- [x] 1.1 新增 `edr environment-scan` CLI 命令入口与参数绑定（`groups`、`hostname`、`ip`、`key`、`value`、`user`、`sysEnv`）。
- [x] 1.2 定义环境变量扫描统一输出模型，包含 `displayIp`、`externalIpList`、`internalIpList` 与环境变量字段集合。
- [x] 1.3 在参数校验层补充 `sysEnv`、数组参数与空值策略校验，统一错误返回格式。

## 2. 跨平台采集实现

- [x] 2.1 实现 Linux 环境变量采集适配器，使用进程内 API/系统数据源读取，不调用外部命令。
- [x] 2.2 实现 Windows 环境变量采集适配器，使用系统 API 读取，不调用外部命令。
- [x] 2.3 实现主机内外网 IP 全量收集并映射为 `externalIpList`、`internalIpList`，保留 `displayIp` 选择逻辑。
- [x] 2.4 实现跨平台字段归一化与空值填充策略，确保键完整不缺失。

## 3. 查询过滤与结果处理

- [x] 3.1 实现 `hostname`、`ip` 的大小写不敏感模糊匹配过滤。
- [x] 3.2 实现 `groups`、`key`、`value`、`user`、`sysEnv` 组合过滤逻辑，并保证“全部条件同时生效”。
- [x] 3.3 为过滤链路添加单元测试，覆盖无过滤、单过滤、多过滤组合场景。

## 4. 输出能力与稳定性

- [x] 4.1 实现 JSON 输出编码，确保与规范字段一一对应。
- [x] 4.2 实现 Excel 输出编码，列定义与 JSON 契约一致。
- [x] 4.3 增加大结果集导出测试，验证 Excel 输出性能与稳定性。

## 5. 验证与文档

- [x] 5.1 增加跨平台集成测试，验证 Linux/Windows 均不触发外部命令执行。
- [x] 5.2 增加能力边界测试，确认结果中不包含风险评分、告警等级等风险分析字段。
- [x] 5.3 更新使用文档与 CLI 示例，补充 JSON/Excel 输出示例和参数说明。
