你是一个高级工具开发工程师和网络安全研究员，帮我添加一下edr工具中主机扫描模块中进程扫描模块的一些内容，要保证windows和linux都可以使用，不要通过命令行来获取这些信息，这块只进行信息收集不进行风险分析，使用go语言来写，输出使用全局命令-o实现，通过命令行来使用该工具，编码使用utf-8，提示和报错信息用英文。

(1)在hostscan的process模块里添加收集主机外联ip的功能，该功能必须要和进程联动进行获取。不用做成单独的请求启用参数，直接把这个功能做到进程扫描模块里，默认之前去收集外联ip，可以添加响应的返回参数。